
export type TGroupListItem = {
  id: string;
  name: string;
  sub_title: string;
  is_active: boolean;
}

export type TListData = {
  id: number;
  name: string;
  description: string;
  is_active: boolean;
};